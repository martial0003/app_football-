name: nom_de_votre_application
description: Une application de football pour suivre les matchs de Ligue 1.
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.1.0
  supabase_flutter: ^2.5.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1 # Dernière version stable

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true